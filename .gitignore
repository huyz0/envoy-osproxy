target/
**/*.rs.bk
# The excluded module crates + the custom-module example build standalone; their
# locks are host/SDK-specific (a git-pinned Envoy SDK), so they are not tracked.
crates/evoxy-module/Cargo.lock
crates/evoxy-module-sdk/Cargo.lock
examples/custom-module/Cargo.lock

